import React from "react";
import ReactDOM from "react-dom";
import "./index.less";
import PropTypes from 'prop-types';

export default function ProblemPanel(props) {
    const {title, tag, children} = props;
    return <div className="problem-panel">
        problem panel
        </div>
}
